Model examples
--------------

These are a collection of examples demonstrating the model prediction
functionality of jl_exp_deconv, as well as how examples can be compiled into
a gallery with `sphinx-gallery`.
